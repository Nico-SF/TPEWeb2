<?php require 'templates/header.phtml' ?>

<section class="products">
    <h2 class="text-center my-5">Productos</h2>
    <div class="container">
        <div class="row">
            <?php $count = 0; ?>
            <?php foreach ($products as $product) : ?>
                <div class="col-4">
                    <div class="card">
                        <img src="<?= $product->imagen_url; ?>" class="card-img-top" alt="" width="300" height="300">
                        <div class="card-body">
                            <h5 class="card-title"><?= $product->producto_nombre ?></h5>
                            <p class="card-text">Categoría: <?= $product->categoria_nombre ?></p>
                            <a href="productos/ver/<?= $product->producto_id; ?>" class="btn btn-primary">Ver detalles</a>
                        </div>
                    </div>
                </div>
                <?php $count++; ?>
                <?php if ($count % 3 == 0) : ?>
                    </div><div class="row">
                <?php endif; ?>
            <?php endforeach ?>
        </div>
    </div>
</section>

<?php require 'templates/footer.phtml' ?>
